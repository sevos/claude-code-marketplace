# Project Configuration

## Project Management

**System**: Local-Markdown
**Directory**: docs/tickets

This project uses local markdown-based ticket management stored in the `docs/tickets/` directory. Each ticket is a markdown file with YAML frontmatter containing metadata.
